import { ITeamProps } from '@/types';
import './Team.css';

const Team = (props: ITeamProps) => {
    return (
        <div
            className={'team-container'}
        >

        </div>
    );
};

export default Team;
